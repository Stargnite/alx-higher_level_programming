-- this is a script that computes the score average of all records in a table
-- this is a script that  computes the score average of all records in second_table
SELECT AVG(score)
AS 'average'
FROM second_table;
